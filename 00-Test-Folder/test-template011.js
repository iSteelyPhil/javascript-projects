array1 = ['One', 'i', 'c', 'X', 'i', 'i', 54].

function removeI(arr) {
    if (arr.includes('i')){
      return
     
    } else {
        arr.indexOf('i') === -1
      
    }
 };
 console.log(removeI(array1))